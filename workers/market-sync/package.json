{
  "name": "@eventrail/market-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@eventrail/database": "workspace:*",
    "@eventrail/dreamdex-adapter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.5.0",
    "typescript": "6.0.3"
  }
}
